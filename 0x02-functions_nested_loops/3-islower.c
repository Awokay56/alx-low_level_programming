#include "main.h"

/**
 * main -checks for lowercase character.
 * Return:1 if c is lowercase or 0 for anything else
 */

int -islower.c(int c)

{

	if (c >= 97 && c <= 122)
	{
		return (1);
	}
		return (0);
}


