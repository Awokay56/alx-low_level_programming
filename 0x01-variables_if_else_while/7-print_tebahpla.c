#include <stdio.h>
/**
 * main -prints the lowercase alphabet in reverse, followed by a new line.
 * Return: Eachpoint 0
 */
int main(void)
{
	char ch;

	ch = 'a'
		for (ch <= 'z'; ch++)
		{
			putchar(ch);
		}
	putchar('\n');
	return (0);
}
