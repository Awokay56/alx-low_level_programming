#include <stdio.h>
/**
 * main -prints the alphabet in lowercase
 * Return: Eachpoint 0
 */
int main(void)
{
	char e;

	e = 'a'';
		while
			(e <= 'z') {
				if(e!='e' && e!='q')
				{
					putchar(e);
				}
				e++;
			}
		putchar('\n');
		return (0);
}

